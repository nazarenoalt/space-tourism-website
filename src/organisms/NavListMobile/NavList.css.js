import styled from "styled-components";

export const Wrapper = styled.div`
position: fixed;
top: 0;
right: 0;
bottom: 0;

width: 254px;
display: hidden;
backdrop-filter: blur(10px);

.ul-container {
  z-index: 10;
}

`;